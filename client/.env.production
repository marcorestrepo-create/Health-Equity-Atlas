# Production build-time environment variables
# VITE_ prefixed vars are baked into the client bundle at build time.
# GA4 measurement IDs are public by design (visible in every page's HTML source).
VITE_GA4_ID=G-ZZ2S3Y0954
